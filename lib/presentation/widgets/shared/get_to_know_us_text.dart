import 'package:flutter/material.dart';

class GetToKnowUsText extends StatelessWidget {
  const GetToKnowUsText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Get to know us',
      style: TextStyle(
        fontSize: 18,
        fontWeight: FontWeight.bold,
      ),
    );
  }
}
